---
title: "2025-11-01"
date: "2025-11-01 20:21:30 +0900"
last_modified_at: "2025-11-01 20:21:30 +0900"
draft: true
---

# 2025-11-01
Claude Code がネイティブインストールになったと聞いて。npm ではなくなった

https://x.com/claudeai/status/1984304957353243061

https://docs.claude.com/en/docs/claude-code/setup

移行する

セッションを全部閉じて
claude install -> brew などで入れてから？

.bashrc or .zshrc

```sh
alias claude="/Users/noriyo_tcp/.claude/local/claude"
```

これは一旦無効にしたほうがよさそう

```
brew install --cask claude-code

(snip)
==> Downloading https://storage.googleapis.com/claude-code-dist-86c565f3-f756-42ad-8dfa-d59b1c096819/claude-code-releases/2.0.31/darwin-x64/claude
########################################################################################################################################### 100.0%
==> Installing Cask claude-code
==> Linking Binary 'claude' to '/usr/local/bin/claude'
🍺  claude-code was successfully installed!
```

うーんなんか変

```
❯ which claude
/Users/noriyo_tcp/.asdf/shims/claude
```

```
❯ npm uninstall -g @anthropic-ai/claude-code

up to date in 380ms
Reshimming asdf nodejs...
```


npm list -g --depth=0 でみてもアンインストールされてるようなんだけどなあ

https://github.com/asdf-vm/asdf/issues/1955

```
❯ asdf which claude
unexpected error: no versions set for claude
```

https://tongfamily.com/2025/08/15/ai-roo-code-claude-code-and-open-webui-missing-manual-2025-07/

これは Roo Code と Claude の連携がおかしくなった？的な生地だけどシンプルに rm ~/.asdf/shims/claude しているな

```
❯ rm ~/.asdf/shims/claude
remove /Users/noriyo_tcp/.asdf/shims/claude? y
```

これでこうなった

```
❯ which claude
/usr/local/bin/claude
```


```
❯ bun upgrade
Bun v1.3.1 is out! You're on v1.2.12
[3.96s] Upgraded.

Welcome to Bun v1.3.1!

What's new in Bun v1.3.1:

    https://bun.sh/blog/release-notes/bun-v1.3.1

Report any bugs:

    https://github.com/oven-sh/bun/issues

Commit log:

    https://github.com/oven-sh/bun/compare/bun-v1.2.12...bun-v1.3.1
```

それでもまだ出るなあ

rm -rf ~/.bun をしてしまったが変わらない。brew で入れていたので一旦アンインストールしよう

brew uninstall bun でも変わらんな 一旦無視しよう

https://github.com/anthropics/claude-code/issues/7107

一旦削除してから brew install したら直ったようだ？

```
rm /usr/local/bin/claude

brew install --cask claude-code
✔︎ JSON API cask.jws.json
✔︎ JSON API formula.jws.json
Warning: Not upgrading claude-code, the latest version is already installed
```

ん〜なんか変やな

which cluade で claude not found なんだけど

```
❯ brew uninstall --cask claude-code
❯ brew install --cask claude-code
==> Caveats
Claude Code's auto-updater installs updates to `~/.local/bin/claude` and
not to Homebrew's location. It is recommended to disable the auto-updater
with either `DISABLE_AUTOUPDATER=1` or
`claude config set autoUpdates false` and use
`brew upgrade --cask claude-code`.

==> Downloading https://storage.googleapis.com/claude-code-dist-86c565f3-f756-42ad-8
############################################################################# 100.0%
==> Installing Cask claude-code
==> Linking Binary 'claude' to '/usr/local/bin/claude'
🍺  claude-code was successfully installed!
```


auto-updater が入るパスが homebrew のロケーションではないので auto-updater を無効にしてから brew upgrade するといいよって言われてる

うーん、それでも bun の警告は出てしまうな。一旦無視だ。auto-updater を無効にしておこう

```
claude config set autoUpdates false
```

claude 立ち上げてから `/status` を見ると Multiple Installation になっていた。 npm のほうはアンインストールしたはずなのだけど。  
というわけで Claude 自身に聞いてみたw

```
Claude Code Installation Cleanup Plan

 Current Situation

 You have a working Homebrew installation of Claude Code, but leftover files from
 the previous npm installation are causing configuration warnings.

 What We'll Do

 Step 1: Remove leftover npm local installation
 - Delete ~/.claude/local/ directory (contains old npm package files)
 - This is safe - it's not used by your Homebrew installation

 Step 2: Reset installation method configuration
 - Run claude install to update internal config
 - This tells Claude Code you're using the native (Homebrew) installation

 Step 3: Verify the cleanup
 - Run claude doctor to check for remaining issues
 - Check /status to confirm warnings are resolved

 What We Won't Touch

 - ✅ Your settings, history, and custom commands (all safe)
 - ✅ The Homebrew installation at /usr/local/bin/claude (despite the misleading
 warning, this is your active installation)

 Expected Result

 - Single installation detected
 - No config mismatches
 - All warnings resolved (except possibly one harmless PATH warning for
 ~/.local/bin which doesn't apply to Homebrew)

 Ready to proceed with cleanup?
```

Step 2 の claude isntall をやってなかったなあ？

`/usr/local/bin/claude` ではなくて `~/.local/bin/claude` に入れてくれるってことかな？  
でもそれによって2箇所にインストールしちゃっているようだ？

```
🔄 現在の状態

- アクティブなClaude: /usr/local/bin/claude (Homebrewインストール) v2.0.31
- 追加でインストールされたもの: ~/.local/bin/claude (native installation) v2.0.31

📊 何が起きたか

claude install
コマンドを実行したとき、設定をリセットするのではなく、新しいnative 
installationを作成してしまいました。これは想定外でしたが、問題ありません。

現在、which claude はHomebrewのインストール (/usr/local/bin/claude)
を指しているので、あなたが使いたいものが有効になっています。

🤔 次の選択肢

2つの選択肢があります：

選択肢A: Homebrewインストールを使い続ける（おすすめ）
- ~/.local/bin/claude と ~/.local/share/claude/ を削除
- Homebrewで管理されたインストールのみを保持
- メリット: brew upgrade で簡単にアップデート可能

選択肢B: Native installationに切り替える
- Homebrewをアンインストール (brew uninstall claude-code)
- ~/.local/bin をPATHに追加
- メリット: 公式の標準インストール方法

どちらをお勧めしますか？個人的には選択肢A（Homebrewを使い続ける）をお勧めしま
す。すでにHomebrewでインストールしていて、それが正常に動作しているからです。
```

https://docs.claude.com/en/docs/claude-code/setup#alternative-installation-methods

`claude install` の実行はあくまで Alternative Installation Methods であって brew install とは違うのかもしれない

```
  修正されたプラン

  つまり、正しいクリーンアップ手順は：

  ## シンプルなクリーンアップ（修正版）

  **Step 1: 誤って作成されたnative installationを削除**
  - `rm -rf ~/.local/bin/claude`
  - `rm -rf ~/.local/share/claude/`

  **Step 2: shellキャッシュをクリア**
  - `rehash` (zsh) or `hash -r` (bash)

  **Step 3: 確認**
  - `which claude` → `/usr/local/bin/claude` を確認
  - Claude Codeを再起動して `/status` を確認

  **settings.jsonは触らない！**
  - `installMethod` はClaude Code内部が管理
  - 手動編集は不要（むしろ危険）

  この修正版のプランで進めてよろしいですか？
```

```
  すべてのタスクが完了しました。確認結果：

  現在の状態

  - ✅ which claude → /usr/local/bin/claude (Homebrew installation)
  - ✅ バージョン: 2.0.31 (Claude Code)
  - ✅ 古いnpm local installationを削除 (~/.claude/local/)
  - ✅ stale asdf shimを削除
  - ✅ 誤って作成されたnative installationを削除 (~/.local/bin/claude,
  ~/.local/share/claude/)
  - ✅ Shellキャッシュをクリア

  次のステップ

  Claude Codeを再起動して、/status
  コマンドで警告が解消されたか確認してください。

  期待される結果

  - 単一のインストールが検出される（Homebrew/native）
  - "Multiple installations detected" 警告が消える
  - "Leftover npm installation" 警告が消える

  残る可能性のある警告

  Issue #5159 https://github.com/anthropics/claude-code/issues/5159 により、~/.local/bin is not in your PATH という警告が残る可能性があ
  りますが、これは無害なバグです。Homebrewインストールは正常に動作します。

  ---
  ★ Insight ─────────────────────────────────────
  1. claude install の誤解:
  このコマンドはnpm→native移行用で、Homebrewユーザーは実行すべきではない
  2. 内部管理の設定: installMethod はsettings.jsonには存在せず、Claude
  Code内部で管理される
  3. Homebrewは "native" 扱い: GitHub Issue
  #5306により、Homebrewインストールは内部的に"native"として扱われることが判明 https://github.com/anthropics/claude-code/issues/5306
  ─────────────────────────────────────────────────

  再起動後、結果を教えてください！🎉
```

```
Claude Code インストール修復プラン（Option A: 設定リセット）

現状分析

- ~/.claude.json の installMethod: "native"
は正しい（Homebrewもnativeとして扱われる）
- 問題：Claude Codeが ~/.local/bin/claude を自動作成し続ける
- 使いたいもの：Homebrewインストール (/usr/local/bin/claude)

実行手順

Step 1: すべてのClaude Codeプロセスをkill
- すべてのClaude Code実行中のプロセスを終了
- これがないと設定が元に戻る可能性がある

Step 2: Native installationを削除
- ~/.local/bin/claude を削除（symlink）
- ~/.local/share/claude/ を削除（バイナリ本体）

Step 3: 古いnpm installationを削除
- ~/.asdf/installs/nodejs/22.9.0/bin/claude を削除
- ~/.asdf/installs/nodejs/22.9.0/lib/node_modules/@anthropic-ai/claude-code/
も削除

Step 4: Shellキャッシュをクリア
- hash -r で実行可能ファイルキャッシュをリセット

Step 5: 確認
- which claude が /usr/local/bin/claude を指すことを確認
- 新しいシェルを開いてClaude Codeを起動
- /status で警告を確認

期待される結果

- ✅ which claude → /usr/local/bin/claude
- ✅ /status で "Single installation detected"
- ✅ ~/.local/bin/claude が再作成されない

懸念事項

もし ~/.local/bin/claude が再び自動作成される場合は、Option
B（完全再インストール）を検討します。

注意

- ~/.claude.json の installMethod は変更しません（既に正しい値）
- 会話履歴やsettings.jsonは保持されます

実行しますか？
```
```
⏺ 問題が発生しました！😓

  /Users/noriyo_tcp/.local/bin/claude -> 
  /Users/noriyo_tcp/.local/share/claude/versions/2.0.31

  削除したはずの ~/.local/bin/claude が即座に再作成されています（タイムスタンプ:
   22:12）。

  これはまさにGitHub issue #10280で報告されている問題です。Claude
  Codeに非常に積極的な自己修復機能があり、削除してもすぐに復活してしまいます。

  次の選択肢

  選択肢A: Option B（完全再インストール）を試す
  - Homebrewをアンインストール
  - すべての設定を削除（会話履歴は事前にバックアップ）
  - Homebrewで新規インストール
  - 成功率：99%だが、設定が失われるリスクあり

  選択肢B: この状態を受け入れて使い続ける
  - 機能的には問題なし（Homebrewが /usr/local/bin/claude
  にあり、それが優先される）
  - /status の警告は診断システムのバグとして無視
  - 使用上の支障はない

  選択肢C: シンボリックリンクの向き先を変更（トリック）
  - ~/.local/bin/claude を削除不可能なので、Homebrewを指すようにする
  - ただし、アップデート時に壊れる可能性あり

  どの選択肢が良いでしょうか？個人的には、選択肢B（現状を受け入れる）が最も安全
  で実用的だと思います。
```

```
  1. 今後 claude install を実行しないでください - これがすべての混乱の原因でした
  2. Homebrewでのアップデート: brew upgrade claude-code を使用
  3. Claude Codeは正常に動作します - 警告は無視してOK

  ---
  ★ Insight ─────────────────────────────────────
  1. 自己修復機能の強さ: Claude
  Codeには削除したファイルを即座に再作成する積極的な自己修復機能がある
  2. claude install の危険性: Alternative installation
  methods用のコマンドで、Homebrewユーザーには不適切
  3. 診断システムの限界: /status
  はHomebrewを正しく認識できず、誤った警告を表示する既知のバグがある
  4. 実用性を優先: 完璧な診断結果よりも、実際に動作することの方が重要
  ─────────────────────────────────────────────────
```

---

やっぱり brew uninstall したほうが良さそうだなあ

```
brew uninstall --cask claude-code
curl -fsSL https://claude.ai/install.sh | bash
```

```
❯ curl -fsSL https://claude.ai/install.sh | bash
Setting up Claude Code...
warn: CPU lacks AVX support, strange crashes may occur. Reinstall Bun or use *-baseline build:
  https://github.com/oven-sh/bun/releases/download/bun-v1.3.1/bun-darwin-x64-baseline.zip

✔ Claude Code successfully installed!

  Version: 2.0.31

  Location: ~/.local/bin/claude


  Next: Run claude --help to get started

⚠ Setup notes:
  • Native installation exists but ~/.local/bin is not in your PATH. Run: echo 
  'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc


✅ Installation complete!
```

```
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc
echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc && source ~/.zshrc
```

